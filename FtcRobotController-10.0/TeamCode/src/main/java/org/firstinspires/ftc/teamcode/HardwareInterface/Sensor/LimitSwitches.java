package org.firstinspires.ftc.teamcode.HardwareInterface.Sensor;

/**
 * Logical switch identifiers for SensorControl. Add entries when the new robot has limit switches,
 * then map each device explicitly; the starter has none.
 */
public enum LimitSwitches {
    ;
}
