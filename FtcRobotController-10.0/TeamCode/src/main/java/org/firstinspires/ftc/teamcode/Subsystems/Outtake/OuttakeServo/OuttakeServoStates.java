package org.firstinspires.ftc.teamcode.Subsystems.Outtake.OuttakeServo;

/** Requests understood by OuttakeServoControl. Add only states needed by the new mechanism. */
public enum OuttakeServoStates {
    idle
}
