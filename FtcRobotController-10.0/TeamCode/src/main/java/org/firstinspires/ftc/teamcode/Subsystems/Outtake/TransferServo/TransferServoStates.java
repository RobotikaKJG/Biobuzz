package org.firstinspires.ftc.teamcode.Subsystems.Outtake.TransferServo;

/** Requests understood by TransferServoControl. Add only states needed by the new mechanism. */
public enum TransferServoStates {
    idle
}
